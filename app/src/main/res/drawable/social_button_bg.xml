<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed State -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#E0E0E0" /> <!-- Slightly darker when pressed -->
            <corners android:radius="12dp" />
            <stroke android:color="#BDBDBD" android:width="1dp" />
        </shape>
    </item>

    <!-- Default State -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- White background -->
            <corners android:radius="12dp" />
            <stroke android:color="#DDDDDD" android:width="1dp" /> <!-- Subtle border -->
            <padding
                android:left="8dp"
                android:top="8dp"
                android:right="8dp"
                android:bottom="8dp" />
            <shadowColor android:color="#1A000000"/>
        </shape>
    </item>

</selector>
